#include <bits/stdc++.h>
using namespace std;
#define i64 long long
#ifdef ONPC
#include "mydebug.h"
#else
#define debug(...) 1
#endif

void solve() {
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n = 1;
  cin >> n;
  for (int i = 1; i <= n; i++) {
//    cout << "Case #" << i << ": ";
    solve();
  }
}
