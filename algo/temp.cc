#include <bits/stdc++.h>
using namespace std;
#define i64 long long
#ifdef ONPC
#include "mydebug.h"
#else
#define debug(...) 1
#endif

void solve() {
}

int32_t main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int t = 1;
	cin >> t;
	for (int i = 1; i <= t; i++) {
		// cout << "Case " << i << ": ";
		solve();
	}
}
